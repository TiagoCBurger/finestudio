'}`);
❌ NOYES' : '✅ ) ? 'lsedModeblnanana' in e-nano-ba('kie visible: ${ano-bananaie-n`  kconsole.log(
}`);ls).lengthdeMoys(enabledject.ke{Obmodels: $d able  Ene.log(`;
consolth}`)odels).leng.keys(imageM: ${Objectl modelslog(`  Tota
console.mary:');g('\n📊 Sumle.lo
conso');
}
elsledModn enab NOT found iog('  ❌le.le {
  consoelsls');
} ModeedablFound in en.log('  ✅ 
  consolels) {nabledMode in ena'-banaanoie-n ('k

if
}geModels');nd in ima ❌ NOT foug(' sole.loon {
  c;
} else].enabled)-banana'nonaie-'kageModels[bled:', imnae.log('  - Econsollabel);
  -banana'].e-nanogeModels['kibel:', imag('  - La.lo
  consoleeModels');magound in i.log('  ✅ Fsole
  conodels) {geM imao-banana' in-nanie');
if ('k-banana:ng kie-nanoheckilog('\n🔍 Csole.});

conel}`);
ab: ${model.l  - ${key}sole.log(`on  c{
odel]) => , mh(([keydels).forEacMonabledes(eObject.entri);

e)
als= fl.enabled !=deel]) => moter(([_, modls).files(imageMode.entris(
  ObjectEntriet.from = ObjecledModelsnab;
const es:')del Enabled mo\n✅sole.log(';

con`);
})})enabledd: ${model.able(enl.label} : ${mode(`  - ${key}log console.l]) => {
 key, modeforEach(([mageModels)..entries(i:');
Object📋 All modelslog('

console.},
};,
  trueled: 
    enab)',ai(Kie.na o Bana: '🍌 Nan    labelanana': {
ie-nano-b
  },
  'kled: false,   enab,
 (BYOK)' Image Edit bel: 'GPT: {
    la-edit't-imagegp,
  'fal-e,
  }tru   default:  true,
 led:   enab',
 mageo-I Dev Image-tbel: 'FLUX  la  : {
age'-imge-tomadev-i  'fal-flux-
  },
,nabled: true  ea',
  ano Bananlabel: '🍌 N   {
  ano-banana':al-n
  'fdels = {imageMo
const jectodels ob the imageM// Simulaten');

ibility...\na model vise Nano Bana KiTesting.log('🔍 sole
conjs
st.ana-linano-bant-kie-: node tesn with/ Rut
/ lis the models appears in-banana kie-nanorify ifcript to vet s// Tes